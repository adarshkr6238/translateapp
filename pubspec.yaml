name: bubble_translator
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_overlay_window: ^0.8.0
  google_mlkit_text_recognition: ^0.13.1
  google_mlkit_translation: ^0.11.0
  google_mlkit_language_id: ^0.10.1
  permission_handler: ^11.3.1
  screen_capturer: ^0.2.1
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
